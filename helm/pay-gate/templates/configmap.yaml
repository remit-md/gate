apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pay-gate.fullname" . }}
  labels:
    {{- include "pay-gate.labels" . | nindent 4 }}
data:
  pay-gate.yaml: |
    {{- .Values.config | nindent 4 }}
