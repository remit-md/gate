apiVersion: v1
kind: Service
metadata:
  name: {{ include "pay-gate.fullname" . }}
  labels:
    {{- include "pay-gate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pay-gate.selectorLabels" . | nindent 4 }}
